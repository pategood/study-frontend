

通过改变 URL，在不重新请求页面的情况下，更新页面视图。

实现方式: 
    1.Hash --- 利用 URL 中的hash("#");

    2.利用 History interface 在HTML5中新增的方法。
  

  Hash
     hash 虽然出现在 url 中，但不会被包括在 http 请求中，
     它是用来指导浏览器动作的，对服务器端完全无用，因此，改变 hash 不会重新加载页面。